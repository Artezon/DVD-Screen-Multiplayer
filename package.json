{
  "name": "dvd-screen-multiplayer",
  "version": "2.0.0",
  "license": "MIT",
  "author": "Artezon",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently --names \"Server,Client\" \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "prod": "npm run build -w frontend && npm run build -w backend && npm run prod -w backend",
    "format": "npm run format -w backend && npm run format -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.3"
  }
}
